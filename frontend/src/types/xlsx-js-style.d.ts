// xlsx-js-style is a styling-capable fork of SheetJS with the same API surface.
// Reuse the xlsx type definitions; cell style objects (`.s`) are set via `any`.
declare module 'xlsx-js-style' {
  export * from 'xlsx'
}
